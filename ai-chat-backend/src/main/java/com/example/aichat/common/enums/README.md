# Enums

Здесь находятся только enum, общие для нескольких задач. Специфичный enum
лежит в `task/<name>/enums`, поэтому изменение одной задачи не затрагивает
остальные. Расширяемые strategy/policy лучше оформлять интерфейсом и registry.

# День 2: универсальный JSON-ответ

Поддомен содержит `AnswerJsonOutputPolicy` и `AnswerPayload`, используемые
режимом строгого JSON универсального профиля. Он отделён от рецептов, поскольку
не использует кулинарный validator и имеет собственную схему `{"answer":"..."}`.

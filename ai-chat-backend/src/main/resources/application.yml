spring:
  application:
    name: ai-chat-backend

server:
  port: 8080

app:
  chat:
    mode: ${CHAT_MODE:fallback}
    base-url: ${LLM_BASE_URL:https://api.deepseek.com}
    api-key: ${LLM_API_KEY:}
    model: ${LLM_MODEL:deepseek-v4-flash}
    agent-profiles-pattern: ${AGENT_PROFILES_PATTERN:classpath*:agents/*.yml}
    default-agent-id: ${DEFAULT_AGENT_ID:general}
    log-payloads: ${LLM_LOG_PAYLOADS:false}
    pricing:
      prompt-cache-miss-per-million-usd: ${LLM_PRICE_INPUT_CACHE_MISS_PER_MILLION_USD:0}
      prompt-cache-hit-per-million-usd: ${LLM_PRICE_INPUT_CACHE_HIT_PER_MILLION_USD:0}
      output-per-million-usd: ${LLM_PRICE_OUTPUT_PER_MILLION_USD:0}
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
